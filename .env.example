# ================================
# Application
# ================================
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ================================
# Database (PostgreSQL)
# ================================
# For Dev Container (services run on localhost)
DATABASE_URL="postgresql://stories_user:stories_pass@localhost:5432/stories_collector?schema=public"

# For Docker Compose Production (services run in separate containers)
# DATABASE_URL="postgresql://stories_user:stories_pass@postgres:5432/stories_collector?schema=public"

# ================================
# Redis
# ================================
# For Dev Container
REDIS_URL=redis://localhost:6379

# For Docker Compose Production
# REDIS_URL=redis://redis:6379

# Redis password (optional)
REDIS_PASSWORD=

# ================================
# Rate Limiting
# ================================
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=10

# ================================
# JWT & OTP Configuration
# ================================
JWT_SECRET=your-super-secret-jwt-key-min-32-chars
JWT_EXPIRES_IN=15m
OTP_CODE_TTL_SECONDS=300
OTP_MAX_ATTEMPTS=5

# ================================
# Future: Phone Verification (TextBee)
# ================================
# TextBee (example)
# Set these if you plan to use TextBee for SMS delivery. If unset, OTPs will
# be printed to the server console (safe for development).
TEXTBEE_BASE_URL=https://api.textbee.example
TEXTBEE_DEVICE_ID=your-device-id
TEXTBEE_API_KEY=your-textbee-api-key

# ================================
# Email / SMTP (example: Zoho Mail)
# ================================
# To send OTPs by email set SMTP_* values and install `nodemailer`.
# Zoho SMTP example:
#   host: smtp.zoho.com
#   port: 465 (SSL) or 587 (TLS)
#   secure: true for port 465, false for 587
SMTP_HOST=smtp.zoho.com
SMTP_PORT=465
# Set to 'true' for port 465 (SSL), 'false' for 587 (STARTTLS)
SMTP_SECURE=true
SMTP_USER=your@yourdomain.com
SMTP_PASS=your-smtp-password-or-app-password
EMAIL_FROM="Stories Collector <no-reply@yourdomain.com>"

# ================================
# Notes: install provider packages
# ================================
# For production sending install these packages in your project:
#   npm install nodemailer axios
# The OTP service will dynamically require them at runtime so they are
# optional during development and in CI when you only need console logging.